# astrbot_plugin_mhhelper v0.3.9

`弱点属性` 的排序与展示改了：不再是「各部位最大值」，而是
**每个属性取吸收最高的 5 行求平均**，再按平均分排序展示前二。

```
### 弱点属性
- 冰(高吸收部位平均吸收20)
- 水(高吸收部位平均吸收15)
```

## 规则

对每个属性，把肉质表里**所有行**（所有部位 / 状态）该属性的吸收值收集起来，
取最高的 5 个求平均 —— 这个平均就是该属性的得分；五个属性算完后按得分排序，
只展示前二。

用户给的例子：某怪各部位的雷吸收为 `10,15,15,15,15,20,5,5,5` →
取前 5（`20,15,15,15,15`）→ 平均 **16** → 雷 的得分。

- 行数不足 5 时按实际行数平均（比如只有 2 行就是两行平均）。
- 平均分 ≤ 0 的属性不算弱点；全 0 时输出「无属性弱点」。
- 同分按 火水雷冰龙 稳定排序。
- **排序用未取整的平均分**，展示时才取整 —— 两个属性都显示 20 但一个是 20.4
  一个是 19.6 时，顺序仍然正确。
- 肉质表最后一列「麻」（实为晕厥）依旧不算属性。

实测煌雷龙：`冰 20 / 水 15 / 火 5 / 龙 5` → 展示前二 `冰(20)`、`水(15)`。

## Changes

- `core/formatter.py` — `_element_weakness()` 改为前 5 平均（返回浮点平均分，
  排序用它）；`_weakness_section()` 标题改 `### 弱点属性`，行格式改为
  `属性(高吸收部位平均吸收xx)`；新增 `_WEAKNESS_TOP_ROWS = 5`。
- README 同步；`PLUGIN_VERSION` → `0.3.9`。

## Tests

- `tests/test_formatter.py` — 弱点相关的 6 个用例按新规则重写，包括用户给的
  例子（前 5 平均 16）、展示格式逐行比对、不足 5 行、全 0、晕厥列排除、同分排序。

Full suite: **186 cases**.

## Upgrade notes

- Reload the plugin (插件页 → 重载插件) 即可，无配置变更。
